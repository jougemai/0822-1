0902
linux多用户多进程:
	基本法则:对于每一个功能,每一个文本,都视为一个文件,进程则是调用这些文件,并根据文件内的程序占用系统的计算及存储资源来执行相应的动作.每一个用户看作是一个目录,用户组看作是一个涵盖了该组用户的大型目录.同时,不同用户不能访问其他用户的文件,不同组之间不能访问对方的文件的原则.

	在此基础上,管理员(root)是系统唯一的管理员,拥有最高的权限.通过管理员来分配给不同组用户不同的空间,同时,只能由管理员来创建新的用户.

	那么,不同的用户在进入到linux系统时,只能调用自己的文件,每一个用户之间互不干扰