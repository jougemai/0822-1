python
循环
	应用场景
		1\爬虫重头解释到尾
	for循环
		可以遍历任何序列的项目,
	while
		continue:执行到这一步时,不再运行此循环内的其他内容,直接跳到下一次循环中
	在格式化输出中
		end表示不换行显示
	关于字符串中的操作
		find\index
			find会返回第一次出现在目标字符串中出现该字符的索引值,没有找到,则返回-1
			index与find类似,单如果找不到的话,会报异常
			区别
				find只能应用在字符串的操作中,
				index不仅能用在字符串中,同样,能应用在元祖\字符串\列表中
		isalpha()和isdigit()
			isalpha()检查字符串是否由字母组成
			isdigit()检查字符串是否由数字组成
		join

	列表的访问
		栈的方式访问
			队列:先进先出,后进后出
			栈  :后进先出,先进后出
				append() 在列表的最后增加元素
				pop()弹出列表最后一个元素
		获取列表的索引
			index() 元素在列表中第一次出现的索引
			enumerate 枚举--通过遍历检测出列表中各元素的索引
		排序
			reverse()--对列表进行改动,会将列表的元素的顺序翻转过来
			sort()--对列表进行操作,

